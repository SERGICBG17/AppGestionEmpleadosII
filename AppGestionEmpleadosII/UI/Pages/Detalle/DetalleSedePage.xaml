<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:AppGestionEmpleadosII.UI.PagesModel"
             x:Class="AppGestionEmpleadosII.UI.Pages.Detalle.DetalleSedePage"
             Title="Información de la Sede">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                <Label Text="{Binding SedeDetalle.Nombre}" 
                       FontSize="32" 
                       FontAttributes="Bold" 
                       HorizontalTextAlignment="Center" />
            </VerticalStackLayout>

            <BoxView HeightRequest="1" />

            <VerticalStackLayout Spacing="5">
                <Label Text="Ubicación y Dirección" FontAttributes="Bold" FontSize="18" />
                <Label Text="{Binding SedeDetalle.Direccion}" FontSize="16" />
            </VerticalStackLayout>

            <VerticalStackLayout Spacing="10">
                <Label Text="Departamentos en esta sede" 
                       FontAttributes="Bold" 
                       FontSize="18" />

                <VerticalStackLayout BindableLayout.ItemsSource="{Binding SedeDetalle.Departamentos}" Spacing="8">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate>
                            <HorizontalStackLayout Spacing="10">
                                <Label Text="•" FontAttributes="Bold" VerticalOptions="Center" />
                                <Label Text="{Binding Nombre}" FontSize="15" VerticalOptions="Center" />
                            </HorizontalStackLayout>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>

                    <BindableLayout.EmptyView>
                        <Label Text="No hay departamentos registrados en esta sede." 
                               FontAttributes="Italic" />
                    </BindableLayout.EmptyView>
                </VerticalStackLayout>
            </VerticalStackLayout>

            <Button Text="Volver al listado"
                    Command="{Binding VolverCommand}"
                    CornerRadius="25"
                    HeightRequest="50"
                    Margin="0,20,0,0" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>